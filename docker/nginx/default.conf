server {
    listen 80;
    server_name localhost;

    location / {
        return 200 'Nginx is running. PostgreSQL is available in another container.';
        add_header Content-Type text/plain;
    }
}
